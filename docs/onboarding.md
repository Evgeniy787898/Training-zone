# 🚀 TZONA V2 — Onboarding Guide

> Добро пожаловать в команду! Этот гайд поможет вам быстро начать работу с проектом.

## 📋 Оглавление

1. [Требования](#требования)
2. [Быстрый старт](#быстрый-старт)
3. [Архитектура](#архитектура)
4. [Разработка](#разработка)
5. [Тестирование](#тестирование)
6. [Деплой](#деплой)
7. [FAQ](#faq)

---

## Требования

### Обязательные

- **Node.js** 20+ (рекомендуется использовать nvm)
- **Python** 3.11+
- **Docker Desktop** (для PostgreSQL и Redis)
- **ngrok** (для Telegram WebApp)

### Проверка окружения

```bash
node -v   # должен быть v20+
python3 --version  # должен быть 3.11+
docker --version
ngrok --version
```

---

## Быстрый старт

### 1. Клонирование

```bash
git clone https://github.com/Evgeniy787898/Training-zone.git
cd Training-zone
```

### 2. Настройка окружения

```bash
# Скопируйте .env.example в .env
cp .env.example .env

# Заполните обязательные переменные:
# - DATABASE_URL (Supabase или локальная PostgreSQL)
# - TELEGRAM_BOT_TOKEN (от @BotFather)
# - JWT_SECRET (любая случайная строка)
# - NGROK_TOKEN (от ngrok.com)
```

### 3. Установка зависимостей

```bash
# Backend
cd backend && npm install

# Frontend
cd ../frontend && npm install

# Python микросервисы
cd ../services/ai-advisor
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

# Повторить для analytics и image-processor
```

### 4. Запуск проекта

```bash
# Из корня проекта
./start-with-ngrok.sh
```

Скрипт автоматически:
- ✅ Проверит все зависимости
- ✅ Запустит ngrok туннель
- ✅ Запустит Docker контейнеры (PostgreSQL, Redis)
- ✅ Запустит Backend, Frontend и микросервисы
- ✅ Покажет Ready баннер со всеми URL

---

## Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                     Telegram WebApp                         │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                      ngrok tunnel                           │
│                   (HTTPS → localhost)                       │
└────────────────────────────┬────────────────────────────────┘
                             │
          ┌──────────────────┼──────────────────┐
          │                  │                  │
          ▼                  ▼                  ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│    Frontend     │ │    Backend      │ │  Microservices  │
│   (Vue + Vite)  │ │ (Express + TS)  │ │    (FastAPI)    │
│   :3000         │ │   :3001         │ │  :3002-3004     │
└─────────────────┘ └────────┬────────┘ └─────────────────┘
                             │
                             ▼
              ┌──────────────────────────┐
              │      PostgreSQL          │
              │      (Supabase)          │
              └──────────────────────────┘
```

### Компоненты

| Порт | Сервис | Описание |
|------|--------|----------|
| 3000 | Frontend | Vue 3 + Vite + TailwindCSS |
| 3001 | Backend | Express.js + Prisma + TypeScript |
| 3002 | Image Processor | FastAPI (обработка изображений) |
| 3003 | AI Advisor | FastAPI (Gemini/OpenAI/Claude) |
| 3004 | Analytics | FastAPI (аналитика тренировок) |

---

## Разработка

### Структура проекта

```
Training-zone/
├── backend/           # Node.js API сервер
│   ├── src/
│   │   ├── routes/    # API endpoints
│   │   ├── services/  # Бизнес-логика
│   │   └── middleware/
│   └── prisma/        # Схема БД
├── frontend/          # Vue 3 приложение
│   ├── src/
│   │   ├── pages/     # Страницы
│   │   ├── components/ # Компоненты
│   │   └── stores/    # Pinia stores
├── services/          # Python микросервисы
│   ├── ai-advisor/
│   ├── analytics/
│   ├── image-processor/
│   └── python_shared/ # Общий код
└── docs/              # Документация
```

### Команды разработки

```bash
# Backend
cd backend
npm run dev        # Запуск в dev режиме
npm run lint       # Линтинг
npm run typecheck  # Проверка типов
npm run test       # Тесты

# Frontend
cd frontend
npm run dev        # Запуск Vite
npm run lint       # Линтинг
npm run type-check # Проверка типов
npm run build      # Продакшн сборка
```

### Git Workflow

1. Создайте ветку от `develop`:
   ```bash
   git checkout develop
   git pull
   git checkout -b feature/your-feature
   ```

2. Коммитьте изменения (pre-commit запустит lint + typecheck):
   ```bash
   git add .
   git commit -m "feat: описание изменений"
   ```

3. Создайте PR в `develop`

---

## Тестирование

### Unit тесты

```bash
# Backend
cd backend && npm run test

# С coverage
npm run test -- --coverage
```

### E2E тесты

```bash
cd frontend
npm install -D @playwright/test
npx playwright test
```

---

## Деплой

Проект использует Supabase для БД и может деплоиться на:
- **Vercel/Netlify** — Frontend
- **Railway/Render** — Backend и микросервисы

Подробнее см. `docs/deployment.md` (TODO)

---

## FAQ

### Ошибка "NGROK_TOKEN not found"

Добавьте токен в `.env`:
```
NGROK_TOKEN=your_token_here
```
Получить токен: [ngrok.com/dashboard](https://dashboard.ngrok.com)

### Ошибка "DATABASE_URL not found"

Убедитесь что `.env` содержит корректный `DATABASE_URL` от Supabase.

### Порт уже занят

Скрипт автоматически освобождает порты 3000-3004. Если проблема сохраняется:
```bash
lsof -ti:3000 | xargs kill -9
```

### Python venv не найден

```bash
cd services/ai-advisor
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

---

## Контакты

- **Telegram бот:** @TZONABot
- **Репозиторий:** [github.com/Evgeniy787898/Training-zone](https://github.com/Evgeniy787898/Training-zone)

---

*Последнее обновление: 2025-12-05*
